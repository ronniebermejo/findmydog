<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  <title><%= content_for?(:title) ? yield(:title) : "Find my dog" %></title>

  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "vendor/modernizr" %>

  <%= csrf_meta_tags %>
</head>

<body ng-app="findMyDogApp">
<%= render :partial => 'app/main_menu' %>
<div ng-controller="findMyDogController">
  <div class="row">
    <div class="large-8 columns">
      <%= image_tag('petfinder-lg-300x297.jpg', align: 'left') %>
      <h3>Como podemos ayudarte?</h3>
      <%= link_to "Buscas a tu perro perdido?", 'report', { :class => "button secondary small" } %>
      <a class="button secondary small" ng-click="search()">Encotraste a un perro?</a>
      <br>
      <small>El lugar perfecto para buscar a tu mascota</small>
      <hr>
    </div>
    <%= render :partial => 'app/search_form' %>
  </div>
  <%= yield %>
</div>
 <%= javascript_include_tag "application" %>
</body>
</html>
